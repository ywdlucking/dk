dependencies {
  compile(
		"io.netty:netty-all:${netty_version}",
		"org.apache.commons:commons-lang3:${apache_common_lang_version}"
  )
  testCompile(
  		"org.springframework:spring-test:${spring_mvc_version}"
  )
}